# Changelog

All notable changes to Rulestatus are documented here.
Format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).
Commits follow [Conventional Commits](https://www.conventionalcommits.org/en/v1.0.0/).

<!-- Generated 2026-05-10 — do not edit manually. Run: bun changelog -->

## [Unreleased]

### Features

- regulatory currency disclaimer on all output surfaces (`b9f2e3c`)
- auditor docs (`82b184b`)
- p2.2 methodology docs (`a40542d`)
- colorado ai act (`068e0ef`)
- framework interop layer (`c9e1dc3`)
- auto generate generate (`51392ba`)
- manual attestations (`cf03538`)
- upgrade cli (`ed1a640`)
- demo repo (`66449e0`)
- better init onboarding (`14a7e75`)
- targeted icp quickstart (`af044a1`)
- node compat (`f533bd9`)
- landing page deployment (`d4d63de`)
- website (`91e628c`)
- website (`57fee75`)
- nist ai rmf support (`55e8c6f`)
- iso 42001 support (`8e45e19`)
- reduce runtime surface api probe (`4b4be15`)
- ts dsl for assertions and obligations (`13fd47a`)
- evidence registry and secret redaction (`a90cdb0`)
- roadmap update (`b73f08a`)
- manual attestation (`7fcbb59`)
- signed artifact upload (`4201f7f`)
- full results bundle (`f6ffd62`)
- junit exports (`f21ea47`)
- setup repomix (`7a0e145`)
- explain command with dynamic last-run context (P1.2) (`96269fd`)
- evidence-readiness framing and liability disclaimers (`0b5c1eb`)
- add generate command and fix console double-render bug (`f18588a`)
- add roadmap (`16832c3`)
- initial setup (`aa537a1`)

### Bug Fixes

- deployment of docs (`8ef7c56`)
- builds in ci (`1987c19`)
- ci usage (`189924d`)
- bug fixes (`c8bedbf`)
- bundle framework modules and add minified/compiled build targets (`72b2e25`)
- resolve lint and typecheck errors in cmdGenerate (`cb98853`)
- cleanup (`30596e5`)

### Refactoring

- reframe P2.7/P2.8 around existing CI and Sigstore infra (`3de823a`)

### Documentation

- assertion validation methodology doc (`69962c7`)
- more references (`ca64fd3`)
- add colorado act (`e2e6753`)
- icp landing page (`fedaa09`)
- more content (`63aa562`)
- add README and add .rulestatus/ to gitignore (`fc5e85e`)

### CI

- generate changelog from commits (`faa4754`)
