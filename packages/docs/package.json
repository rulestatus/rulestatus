{
  "name": "@rulestatus/docs",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "bun scripts/generate-docs.ts",
    "dev": "bun scripts/generate-docs.ts && bunx astro dev",
    "build": "bun scripts/generate-docs.ts && rm -rf .astro && bunx astro build",
    "preview": "bunx astro preview",
    "astro": "bunx astro"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.34.0",
    "astro": "^5.0.0",
    "sharp": "^0.33.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  }
}
